# Dependabot keeps the Maven dependency tree current. Weekly cadence to
# keep PR volume manageable; review and merge the bumps as they land.
version: 2
updates:
  - package-ecosystem: "maven"
    directory: "/"
    schedule:
      interval: "weekly"
