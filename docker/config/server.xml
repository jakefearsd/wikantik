<?xml version="1.0" encoding="UTF-8"?>
<Server port="-1" shutdown="SHUTDOWN">
  <Listener className="org.apache.catalina.startup.VersionLoggerListener" />
  <Listener className="org.apache.catalina.core.AprLifecycleListener" SSLEngine="on" />
  <Listener className="org.apache.catalina.core.JreMemoryLeakPreventionListener" />
  <Listener className="org.apache.catalina.mbeans.GlobalResourcesLifecycleListener" />
  <Listener className="org.apache.catalina.core.ThreadLocalLeakPreventionListener" />

  <GlobalNamingResources>
    <Resource name="UserDatabase" auth="Container"
              description="User database that can be updated and saved"
              factory="org.apache.catalina.users.MemoryUserDatabaseFactory"
              pathname="conf/tomcat-users.xml"
              type="org.apache.catalina.UserDatabase" />
  </GlobalNamingResources>

  <Service name="Catalina">
    <!-- HTTP only — Cloudflare terminates TLS.
         maxThreads = 400 / acceptCount = 200 reverted 2026-05-21 after the
         N=650 mmap load test: bumping to 600/400 oversubscribed a CPU-bound
         16-core host (context-switch + scheduler overhead exceeded the
         throughput gain). DBCP stays bumped to 90 — that's the harmless half
         of the pair (idle conns cost ~10MB each, host has memory headroom). -->
    <Connector port="8080"
        protocol="HTTP/1.1"
        connectionTimeout="20000"
        address="0.0.0.0"
        maxThreads="400"
        acceptCount="200"
        xpoweredBy="false"
        allowTrace="false" />

    <Engine name="Catalina" defaultHost="localhost">
      <Realm className="org.apache.catalina.realm.LockOutRealm">
        <Realm className="org.apache.catalina.realm.UserDatabaseRealm"
               resourceName="UserDatabase"/>
      </Realm>

      <Host name="localhost" appBase="webapps"
            unpackWARs="false" autoDeploy="false"
            deployXML="false">

        <!-- Suppress the Tomcat version banner and stack-trace bodies on error pages.
             NOTE: showReport/showServerInfo are NOT valid <Host> attributes (StandardHost has no
             such setters — the Digester silently ignores them), so they must live on an explicit
             ErrorReportValve. They were previously on <Host> above and had no effect. -->
        <Valve className="org.apache.catalina.valves.ErrorReportValve"
               showReport="false" showServerInfo="false" />

        <!-- Extract real client IP from the reverse-proxy header. Header name is
             parameterized: entrypoint.sh always sets the
             wikantik.proxy.remoteIpHeader system property (default
             CF-Connecting-IP for Cloudflare; PROXY_REMOTE_IP_HEADER overrides,
             e.g. X-Forwarded-For for Caddy/nginx/ALB/GCLB). Tomcat substitutes
             ${...} in server.xml attributes from system properties at boot. -->
        <Valve className="org.apache.catalina.valves.RemoteIpValve"
               remoteIpHeader="${wikantik.proxy.remoteIpHeader}"
               protocolHeader="X-Forwarded-Proto" />

        <Valve className="org.apache.catalina.valves.AccessLogValve" directory="logs"
               prefix="access" suffix=".log"
               pattern="%h %l %u %t &quot;%r&quot; %s %b %D %{CF-IPCountry}i &quot;%{User-Agent}i&quot;" />

        <Valve className="org.apache.catalina.valves.ErrorReportValve"
            showServerInfo="false" showReport="false"/>
      </Host>
    </Engine>
  </Service>
</Server>
