# Math Expression Test

Here is inline math: $E = mc^2$ and $x^2 + y^2 = r^2$.

Here is a display math block:

$$
\int_0^1 f(x)\,dx = F(1) - F(0)
$$

And a fenced math block:

```math
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
```

A literal dollar sign: \$100.

## Math in a list

- The quadratic formula: $x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$
- Euler identity: $e^{i\pi} + 1 = 0$

> Blockquote with math: $\alpha + \beta = \gamma$
