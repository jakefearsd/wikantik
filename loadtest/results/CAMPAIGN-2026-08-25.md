# Profiling campaign 2026-08-25 — CPU reduction with admin traffic in the mix

Host: local bare-metal Tomcat 11, 10 cores, JVM pinned -Xms2g -Xmx2g G1.
k6 co-resident (so absolute RPS is k6-confounded; the signal is CPU
composition + p95, per the §15 precedent in docs/ScalingCharacterization.md).
Corpus 1201 pages / 1635 chunks / 1635 embeddings (qwen3-embedding-0.6b,
dim 1024) against a local CPU ollama. Dense retrieval live.

Mix: 3m @ 40 read VUs + 10 admin VUs (`bin/profile-iteration.sh`).
This is the first campaign whose mix contains /admin/* traffic at all.

## Iteration 0 — baseline

| Metric | Value |
|---|---|
| RPS | 50.5 |
| median | 1.93 ms |
| p90 | 336.8 ms |
| p95 | 428.1 ms |
| max | 4.29 s |
| http_req_failed | 2.67% |

CPU by deepest application frame (19,317 ExecutionSamples):

| % | Frame |
|---|---|
| **96.04** | **com.wikantik.util.CryptoUtil.verifySaltedPassword** |
| 0.55 | com.wikantik.search.hybrid.LuceneHnswChunkVectorIndex.topKChunks |
| 0.38 | com.wikantik.parser.markdown.MarkdownParser.parse |
| 0.34 | com.wikantik.api.frontmatter.FrontmatterParser.parseYaml |
| 0.24 | com.wikantik.parser.markdown.MarkdownParser.collectLinks |

Leaf frame: 95.88% `at.favre.lib.crypto.bcrypt.BCryptOpenBSDProtocol.key`.

Allocation (22,323 samples) — masked by bcrypt in the CPU view:

| % | Frame |
|---|---|
| 41.15 | com.wikantik.search.hybrid.LuceneHnswChunkVectorIndex.topKChunks |
| 6.72 | com.wikantik.search.subsystem.lucene.DefaultLuceneSearcher.findPages |
| 4.85 | com.wikantik.search.subsystem.lucene.LuceneHitSupport.readPageId |
| 4.42 | com.wikantik.api.frontmatter.FrontmatterParser.parseYaml |
| 4.30 | com.wikantik.api.frontmatter.FrontmatterParser.split |

Monitor contention: only 59 events, 51% in `AbstractApiAccessFilter.doFilter`.

The bimodal latency (median 1.93 ms, p95 428 ms) is the bcrypt tax: cached
reads answer in ~2 ms, every Basic-authed admin request pays a full bcrypt.

## Iteration 1 — BasicAuthFilter session fast path was dead code

`BasicAuthFilter` compared the Basic-auth username against
`Session.getUserPrincipal()`, but `WikiSession.injectUserProfilePrincipals()`
sets that to the user's **FullName** (preferring `WikiPrincipal.FULL_NAME`);
the login name lives on `getLoginPrincipal()`. So `"testbot".equals("Test
Automation Bot")` — false on every request, for every user whose full name
differs from their login name. The "don't re-login on every request" guard had
never fired in production.

Fixed by matching either principal. Measured on a session-holding client:
**312 ms -> 1.6 ms** per admin request.

The aggregate profile did NOT move (96.01% still bcrypt), which was the
informative part: the harness — like monitoring pollers, cron jobs, CI scripts
and SDK one-shot calls — is stateless and carries no session cookie, so it
cannot use the fast path at all. That client class is the one that costs money.

## Iteration 2 — short-TTL successful-verification cache

`AbstractUserDatabase.validatePassword` now caches successful verifications,
mirroring `ApiKeyService.verifyCache` (§14.2 of ScalingCharacterization).

- Key: `loginName + ' ' + sha256Hex(password || storedHash)`. Binding the key
  to the stored hash means a password change rotates the key, so it takes
  effect **immediately** rather than after the TTL.
- Successes only — bcrypt's cost is a deliberate brute-force deterrent, so a
  wrong password always pays full price.
- Legacy-hash migration branch excluded (it mutates and saves the profile).
- Kill switch: `wikantik.auth.password.verifyCache.ttlSeconds=0`.
- Cannot bypass lockout: `UserDatabaseLoginModule` checks `profile.isLocked()`
  after and outside `validatePassword()`, against a freshly-fetched profile.

| Metric | Baseline | Iteration 2 | Δ |
|---|---|---|---|
| RPS | 50.5 | 53.8 | +6.7% |
| avg | 92.2 ms | 18.2 ms | **−80%** |
| p90 | 336.8 ms | 31.2 ms | **−91%** |
| p95 | 428.1 ms | 55.4 ms | **−87%** |
| **on-CPU samples (same load)** | **19,317** | **1,345** | **−93%** |
| bcrypt samples | 18,552 | 159 | **−99.1%** |
| stateless admin request | 369 ms | 7.4 ms | **−98%** |

The on-CPU sample count is the headline for cloud cost: the same offered load
now costs **1/14th** of the CPU.

## The profile after iteration 2

With bcrypt gone the real composition is finally visible:

| % | Frame |
|---|---|
| 22.68 | com.wikantik.search.hybrid.LuceneHnswChunkVectorIndex.topKChunks |
| 12.42 | (JDK/container/library only) |
| 11.82 | com.wikantik.util.CryptoUtil.verifySaltedPassword (residual: cache misses + TTL expiry) |
| 6.25 | com.wikantik.api.frontmatter.FrontmatterParser.parseYaml |
| 5.58 | com.wikantik.parser.markdown.MarkdownParser.parse |
| 3.42 | com.wikantik.parser.markdown.MarkdownParser.collectLinks |
| 3.05 | com.wikantik.search.subsystem.lucene.DefaultLuceneSearcher.findPages |

A read-only probe (no admin traffic) puts `topKChunks` at 40.8% of read-path
CPU, and it is 41% of ALL allocation — the largest allocator in the process.
That is iteration 3.

## Iteration 3 — HNSW docvalues acquired per leaf, not per hit

`LuceneHnswChunkVectorIndex.topKChunks` called
`ctx.reader().getSortedDocValues(...)` **twice per hit**. That is not a cheap
accessor — each call constructs a fresh `Lucene90DocValuesProducer$TermsDict`
with its own block buffers. At the shipped `wikantik.bundle.dense.top_k = 300`
that is up to **600 TermsDict constructions per query**, and it measured as the
single largest allocator in the process (`TermsDict.<init>` alone = 11.16% of
ALL allocation samples).

The per-hit acquisition existed for a real reason, documented in the code: KNN
hits arrive in SCORE order, `advanceExact` is forward-only, so one shared
iterator would seek backwards. The fix is to satisfy that contract properly
rather than by re-acquiring — resolve the docvalues in **docid order** (one
forward-only iterator per leaf), then emit in the original **score order**.

The result list is identical by construction, not merely by test: same doc set,
same `sd.score`-derived cosines, same selection rule (first k that resolve, in
score order). Work stays O(k) rather than O(fetch) via batching.

Note this is NOT the stored-fields/LZ4 bug of §14.1 and §15 — this class already
used DocValues correctly. It is the adjacent anti-pattern: re-acquiring the
accessor per document instead of per segment.

| Metric | Iter 2 | Iter 3 | Δ |
|---|---|---|---|
| topKChunks allocation | 37.61% (8329) | 4.26% (863) | **−90%** |
| `TermsDict.<init>` allocation | 11.16% (2471) | absent | **−100%** |
| topKChunks CPU samples | 305 | 159 | **−48%** |
| total on-CPU samples | 1345 | 1102 | −18% |
| p95 | 55.4 ms | 53.7 ms | −3% |
| max | 3.35 s | 1.49 s | **−55%** |

Guarded by two new tests in `LuceneHnswChunkVectorIndexTest` that pin score
order and the id→page mapping across many hits whose docid order is the reverse
of their score order. Mutation-checked: emitting in docid order instead of score
order fails 3 tests. `RetrievalQualitySmokeTest$ParityGate` (including
`inmemory_and_lucene_hnsw_within_recall_epsilon`) passes 6/6.

---

# Campaign summary — baseline → iteration 3

| Metric | Baseline | Final | Δ |
|---|---|---|---|
| RPS | 50.5 | 53.6 | +6% |
| avg latency | 92.2 ms | 16.8 ms | **−82%** |
| p90 | 336.8 ms | 30.2 ms | **−91%** |
| p95 | 428.1 ms | 53.7 ms | **−87%** |
| max | 4.29 s | 1.49 s | −65% |
| **on-CPU samples at equal load** | **19,317** | **1,102** | **−94%** |
| stateless admin request | 369 ms | 7.4 ms | −98% |

**The same offered load now costs ~1/18th of the CPU.** For a cloud deployment
that is the headline: the CPU headroom that was being spent re-deriving a bcrypt
hash on every authenticated request, and rebuilding Lucene docvalue accessors
per search hit, is now available for real work.

## What is left (next campaign)

The profile is no longer dominated by any single frame — the remaining top
entries are diffuse:

| % CPU | Frame | Note |
|---|---|---|
| 14.4 | `LuceneHnswChunkVectorIndex.topKChunks` | now mostly inherent HNSW search + the String/UUID decode chain |
| 13.3 | `CryptoUtil.verifySaltedPassword` | residual: cache misses and 60s TTL expiry. Raising the TTL trades staleness for CPU |
| 8.3 + 3.6 + 1.7 | `FrontmatterParser.{parseYaml,split,loadStrict}` | 13.6% combined — frontmatter is still re-parsed more than it is cached |
| 5.4 + 3.4 | `MarkdownParser.{parse,collectLinks}` | render path |

Ranked candidates for a follow-up:
1. **Store chunk UUIDs as raw 16 bytes** in the HNSW index instead of a 36-char
   string, and rebuild via `new UUID(msb, lsb)` — removes `utf8ToString()` +
   `UUID.fromString()` per hit. Index is derived/rebuildable, so no migration.
2. **Frontmatter parse caching** — 13.6% of CPU across three frames suggests the
   `FrontmatterMetadataCache` is being bypassed on some paths, the same class of
   defect as §15 fix #3.
3. **Password verify cache TTL** — a deliberate staleness/CPU tradeoff, an
   operator decision rather than a code fix.

---

## Full gate (2026-08-25, after iteration 3)

`bin/run-tests.sh --parallel 4` — **RESULT: ALL PASSED**, 10m18s.
Phase 1 unit reactor 5m45s; IT parallel x4 4m29s. Nothing to fix.

## Iteration 4 — `FrontmatterParser.split` copied the whole body to test 4 chars

`split()` tested for the empty-frontmatter case with
`CLOSING_IMMEDIATE.matcher( text.substring( yamlStart ) )`. That `substring`
copied the **entire remaining page body** into a new String on every call, for
every page, purely to check whether `---\n` sat at that offset — then discarded
it. Replaced with `String.startsWith(prefix, offset)`, which allocates nothing.
`CLOSING_IMMEDIATE` was also a byte-identical duplicate of `OPENING` and is gone.
Net: less code.

| | Iter 3 | Iter 4 | Δ |
|---|---|---|---|
| `split()` allocation | 8.50% (1722) | 4.11% (844) | **−51%** |
| `split()` CPU | 3.63% (40) | 2.95% (35) | −13% (within noise) |

Honest reading: the copy was memory **bandwidth**, not compute, so the win lands
in allocation/GC pressure, not on-CPU samples or latency. Total CPU samples went
1102 → 1187 across the two runs — run-to-run variance at these sample counts, not
a regression. Guarded by `FrontmatterParserTest` (empty-block LF + CRLF,
triple-dash-in-body); mutation-checked (a wrong delimiter length fails the suite).

**Gotcha for future work:** `wikantik-main`'s `FrontmatterParserTest` exercises the
*installed* `wikantik-api` jar, not the source tree. Only the `wikantik-api` suite
actually guards that class — mutate-and-test there.

## Considered and deliberately NOT done

**Route `/admin/knowledge-graph/pages-without-frontmatter` through
`FrontmatterMetadataCache`.** `KgMaintenanceAdminHandlers.handleGetPagesWithoutFrontmatter`
does `getAllPages()` + `getPureText()` + `FrontmatterParser.parse()` for the whole
corpus on **every GET**, and only needs `metadata().isEmpty()` — textbook cache
bypass, the same shape as §15 fix #3.

Rejected on cost/benefit: the cache is only reachable via
`engine.getManager(FrontmatterMetadataCache.class)`, and `DecompositionArchTest`
declares "no new `WikiEngine#getManager` callers — services must receive their
dependencies" as a **permanent** rule. The alternative is threading a
`Supplier<FrontmatterMetadataCache>` down through `AdminKnowledgeResource`'s
construction — real cross-layer plumbing. The endpoint is 1 of 10 audit-tier
surfaces (~1% of admin requests). Not worth the architectural churn; revisit only
if the dependency arrives there for another reason.

## Iteration 5 — display-math regex short-circuit

`DisplayMathPreProcessor.transform` ran a DOTALL/MULTILINE backtracking regex over
the whole page body on every parse, including the common case of a page with no
math at all. Added `if ( !source.contains( "$$" ) ) return source;` — output-identical
(no match ⇒ `appendTail` returns `source` verbatim), one line, no new state.

Worth calling out honestly: at 9 samples this is **below the run-to-run noise floor**
(identical configs measured 971–1187 total CPU samples across this campaign), so it
cannot be individually attributed. It is a provably-correct reduction in work, kept
on that basis, not on a measured delta.

## The markdown "render" hotspot is actually the SEARCH path

The largest remaining cluster looked like page rendering. Attributing whole stacks
rather than leaf frames showed otherwise:

| | samples | share |
|---|---|---|
| markdown parse/collectLinks total | 164 | 13.8% of all CPU |
| ...reached via `DefaultLuceneSearcher.findPages` | **157** | **96% of those** |
| ...via `DefaultContextRetrievalService` | 4 | |
| ...page rendering and everything else | 3 | |

`DefaultLuceneSearcher.findPages` calls `pm.getPage()` **per hit** (line ~311).
That routes to `CachingProvider.getPageInfo` → `refreshMetadata`, which does a
FULL extension-laden markdown parse (plus a second bare parse inside
`collectLinks`) to populate page metadata — guarded only by `page.hasMetadata()`,
a flag on the `Page` **instance**. The page-object cache recycles instances on a
60s TTL, so every search re-parses the markdown of every hit it touches.

The search loop only uses the page for its ACL and `lastModified`. It never needs
the parsed metadata.

**Not fixed here, deliberately.** All three of `getPage`/`getPage(v)`/`getPageInfo`
funnel through `refreshMetadata`; there is no metadata-free accessor. Adding one is
a `PageManager` interface change across api → main → repository, and making
metadata materialisation lazy changes `[{SET}]` variable semantics app-wide. That
is a project with its own plan, not a minor edit — the same call the 2026-07-03
tier-1 plan made about the `collectLinks` double-parse. **It is now the single
largest remaining item: ~13% of CPU.**

Also verified already-fixed and still in place (no action needed): flexmark
extensions are `static final` and shared (tier-1 Task 6); `DefaultAclManager`'s
version-keyed `aclCache` (tier-1 Task 3); `isLikelyWikiSyntax` hash memoisation.
No per-call `Pattern.compile` remains anywhere in that cluster.

---

# FINAL — baseline → iteration 5

| Metric | Baseline | Final | Δ |
|---|---|---|---|
| RPS | 50.5 | 54.8 | +8.5% |
| avg latency | 92.2 ms | 14.4 ms | **−84%** |
| p90 | 336.8 ms | 28.1 ms | **−92%** |
| p95 | 428.1 ms | 47.0 ms | **−89%** |
| max | 4.29 s | 1.46 s | −66% |
| failure rate | 2.67% | 2.06% | −23% |
| **on-CPU samples at equal load** | **19,317** | **971** | **−95%** |

**The same offered load costs ~1/20th of the CPU.**

Remaining profile (nothing dominant left):

| % | Frame | Note |
|---|---|---|
| 15.1 | `CryptoUtil.verifySaltedPassword` | cache misses + 60s TTL expiry — an operator dial, not a bug |
| 10.9 | `FrontmatterParser.parseYaml` | inherent SnakeYAML load |
| 10.7 | `LuceneHnswChunkVectorIndex.topKChunks` | inherent HNSW + the String/UUID decode per hit |
| 8.0 + 4.3 | `MarkdownParser.parse` / `collectLinks` | 96% of it via the search path — see above |

## Ranked next steps

1. **Search re-parsing markdown per hit (~13% CPU)** — the big one. Needs a
   metadata-free page accessor or lazy metadata materialisation. Real project.
2. **`collectLinks` double-parse (~4%)** — a second full flexmark parse of the same
   body. Removing it needs the link-mutator chain re-plumbed through
   `WikantikNodePostProcessorFactory`; scoped out by the 2026-07-03 plan for the
   same reason. Largely subsumed by (1) if search stops parsing at all.
3. **Raw 16-byte chunk UUIDs in the HNSW index** — drops `utf8ToString()` +
   `UUID.fromString()` per hit. Index is derived/rebuildable, so no migration.
4. **Password-verify cache TTL** — staleness/CPU dial, operator's call.

## Operational lesson — do not run the gate against a live profiling stack

The first `--parallel 4` run after iteration 5 failed two ITs:
`OntologyPublicEndpointsIT` ("public page must appear in the dump") and
`StructuredFrontmatterEditorIT.rawYamlRoundTrip` (Selenium `TimeoutException`).
Neither was a code defect. The deployed Tomcat and the dev ollama on 11434 were
still running and competing with the IT containers, the shared test embedder on
11435, and the Selenide browser — the log shows embedder retry storms and an
"ontology rebuild already RUNNING" race. With the profiling stack stopped and the
SAME code, the rest module passed 254/254 and the full IT phase passed clean, in
**4m30s vs the failing run's 6m01s**. Stop the profiling stack before gating.

---

## Iteration 6 — the harness was under-measuring the agent surface by 40x

Investigating "the MCP path to search" turned up a harness defect first.

The read mix drove `/wikantik-admin-mcp` with `read_page`, `search_knowledge`
and `query_nodes`. Profiling attributed **0.4% of CPU** to MCP, which looked like
"the agent surface is cheap." It is not — **two of those three tools are no-ops on
this corpus.** `search_knowledge` and `query_nodes` query **Knowledge Graph nodes
only** (pure JDBC against `kg_nodes`, no page store, no retrieval); with no
extracted entities they return `{"results":[]}` in ~3 ms. The tool says so itself:

    "search_knowledge searches Knowledge Graph nodes only; if you expected
     page-body matches, call retrieve_context with the same query."

The real agent retrieval tools live on **`/knowledge-mcp`**, which the harness
never called: `retrieve_context`, `assemble_bundle`, `read_pages`, `get_briefing`.

Measured directly (3 calls each):

| tool | min | avg | max |
|---|---|---|---|
| `retrieve_context` | 9 ms | 607 ms | **1801 ms** |
| `assemble_bundle` | 19 ms | 26 ms | 36 ms |
| `read_pages` | 2 ms | 2 ms | 3 ms |
| `search_knowledge` | 3 ms | 3 ms | 4 ms |

`retrieve_context`'s 9 ms warm vs 1801 ms cold is exactly the `refreshMetadata`
signature: a cold call re-parses the markdown behind every hit; a warm one finds
`hasMetadata()` already set.

`knowledgeAgentFlow` now drives `/knowledge-mcp` (55% `retrieve_context`,
30% `assemble_bundle`, 15% `read_pages`) at 20% of read iterations.

### Corrected attribution

| Frame | broken mix | real mix |
|---|---|---|
| **MCP tool dispatch** | **0.4%** | **16.1%** |
| `ContextRetrievalService` | 31.5% | 33.1% |
| `LuceneHnswChunkVectorIndex.topKChunks` | 10.7% | **21.4%** |
| `CachingProvider.refreshMetadata` | 15.3% | 7.2% |

**A load mix that calls the wrong tool does not under-measure a surface by a
little — it hid 16% of CPU behind an empty result set.** Same lesson as the admin
surface at the start of this campaign, one layer down.

## Answering "is the MCP path to search affected by the markdown re-parse?"

Traced end to end:

| MCP tool | path | hits the per-hit parse? |
|---|---|---|
| `search_knowledge` | `SearchKnowledgeTool` → `KgNodeRepository.queryNodes` — pure JDBC on `kg_nodes` | **No** — never touches the page store |
| `query_nodes` | same | **No** |
| `retrieve_context` | → `ContextRetrievalService` → `SearchManager.findPages` → `DefaultLuceneSearcher.findPages` | **Yes** — same bottleneck as `/api/search` |
| `read_page` / `read_pages` | `pm.getPage()` per slug (`getPureText()` does NOT re-trigger it) | **Yes** — exactly one parse per page |

So the *KG* search tools are clean; the *retrieval* tool shares `/api/search`'s
bottleneck. A fix at `findPages` covers both surfaces.

`/api/search` actually pays it **twice**: once per hit inside `findPages`
(`DefaultLuceneSearcher.java:311`), and again in
`DefaultAuthorizationManager.filterViewable` (`:188`), which loops
`pageManager().getPage(name)` per result — batch in call-shape only.

### The parse is dead weight for these callers — verified

- **ACL** does not need it: `DefaultAclManager.getPermissions` uses its own
  `(name, version, lastModified)`-keyed cache, and on a miss reads `getPureText()`
  + a regex scan for `[{ALLOW}]`. No flexmark.
- **Recency** uses `page.getLastModified()` — a provider-native field.
- **Retrieval metadata shaping** goes through `FrontmatterMetadataCache`, not
  `page.getAttribute()`.
- The only consumer of the attributes this parse populates is **page rendering**
  (`WikiContext` ALIAS/REDIRECT, `DefaultVariableManager` `$var` substitution) —
  and, at index time, `DefaultLuceneIndexer:509` reading `keywords`. **The indexer
  genuinely needs it and must keep it.** Nothing on the search path reads them.

## Iteration 7 — a metadata-free page accessor for the search path

Added, all additive (nothing restructured, no existing caller affected):

- `PageManager.getPageWithoutMetadata(String,int)` — a **default** method delegating
  to `getPage`, so every existing implementation keeps working unchanged.
- `PageProvider.getPageInfoNoMetadata(String,int)` — same shape, needed because
  `DefaultPageRepository` holds the interface type, not `CachingProvider`.
- `CachingProvider` — the shared lookup factored into one private `resolvePageInfo`
  so the two paths cannot drift; the ONLY difference is whether `refreshMetadata`
  runs afterwards.
- Two call sites switched: `DefaultLuceneSearcher.findPages` and
  `DefaultAuthorizationManager.filterViewable`. `DefaultLuceneIndexer` deliberately
  keeps `getPage()` — it reads `page.getAttribute("keywords")` at index time and
  genuinely needs the parse.

Measured against **iteration 6** (same mix — iteration 6 changed the traffic mix,
so it is the correct baseline, not iteration 5):

| Metric | Iter 6 | Iter 7 | Δ |
|---|---|---|---|
| **`findPages` CPU samples** | **112 (10.9%)** | **40 (4.0%)** | **−64%** |
| markdown parse/collect | 72 (7.0%) | 66 (6.6%) | −8% |
| p90 | 30.4 ms | 24.5 ms | **−19%** |
| p95 | 50.0 ms | 44.3 ms | **−12%** |
| RPS | 54.0 | 54.7 | +1% |
| total CPU samples | 1032 | 1001 | −3% (within noise) |

Search results verified byte-identical before and after.

### Why the aggregate moved less than the call-site win

`refreshMetadata` is still 7.8% — but it is no longer *waste*. Attributing the
remaining samples by caller:

| caller | share of remaining `refreshMetadata` |
|---|---|
| **`/api/pages`** | **74.4%** |
| `/wiki` raw | 10.3% |
| retrieval / ContextRetrieval | 7.7% |
| MCP `read_page(s)` | 3.8% |

`/api/pages` is page **rendering**, which legitimately consumes the attributes this
parse populates (`WikiContext` ALIAS/REDIRECT, `DefaultVariableManager` `$var`) —
that is the work the parse exists to do, and it is 40% of the read mix. The
targeted waste, the per-hit parse inside `findPages`, is down 64%.

The win is proportionally larger in a search-heavy mix: `refreshMetadata` measured
15.3% of CPU in the iteration-5 mix versus 7.2% in the agent-inclusive one.

### Note on the interface change and mocks

Mockito **intercepts default interface methods** — a mocked `PageManager` returns
`null` from `getPageWithoutMetadata` rather than running its delegating body, so
tests that stub only `getPage(...)` silently dropped every search hit
(`LuceneSearchProviderCITest`, 7 failures). Fixed with one `lenient()` forwarding
stub in that class's `setUp()` so the mock mirrors production. Worth knowing before
adding any further default method to a heavily-mocked interface.

Gate: full `bin/run-tests.sh --parallel 4` **ALL PASSED** (unit 5m03s + IT 5m04s);
18,756 unit tests, 0 failures. `SearchPermissionTest.testSearchExcludesAclRestrictedPages`
— the test proving ACL filtering still works on an unparsed page — passes.

### Remaining, ranked

1. **`read_page` / `read_pages` (MCP)** — still call `pm.getPage()` per slug and pay
   one parse each; they need version/lastModified only. Now a one-line change each,
   since the accessor exists. Small but free.
2. **`topKChunks` at ~19%** — inherent HNSW plus a `utf8ToString()` + `UUID.fromString()`
   per hit. Storing raw 16-byte UUIDs in the index would remove the decode chain;
   the index is derived/rebuildable so there is no migration.
3. **`verifySaltedPassword` at ~14%** — cache misses and 60s TTL expiry. An operator
   dial, not a defect.
4. **`collectLinks` double-parse** — still needs the link-mutator chain re-plumbed;
   unchanged assessment.

---

## Iteration 8 — numeric chunk-id docvalues + bcrypt herd collapse (2026-09-01)

Items 1–3 above, all three shipped. Measured against iteration 7 (same harness
invocation: `3m @ 40 read / 10 admin VUs`).

### Where iteration 7's CPU actually was

Ranking by "deepest `com.wikantik` frame" names the *function*, not the *reason*.
Re-attributing every sample **inside** `topKChunks` by what sat above it changed
the conclusion, and it is worth repeating the technique:

| phase inside `topKChunks` | iter 7 | iter 8 |
|---|---:|---:|
| docvalues resolve (`lookupOrd` → String) | **56.3%** (107) | **4.4%** (3) |
| HNSW graph search (the actual ANN work) | 36.3% (69) | **82.4%** (56) |
| `UUID.fromString` | 1.6% (3) | 0 |

The item-2 hypothesis above — "the `UUID.fromString` decode chain" — was **wrong**,
and the profile says so plainly: the parse was 1.6% of the frame. The cost was the
`SortedDocValues` **term-dictionary lookup** feeding it. `chunk_id` is a UUID, so it
is unique per document: a sorted term dict over it has cardinality equal to the doc
count, and every `lookupOrd` binary-searches and LZ4-decompresses a dictionary block
to rebuild a 36-char string that is then parsed straight back into the same 128 bits.
`SortedDocValues` is a structure for *low*-cardinality fields; this was the wrong one.

**Fix:** store the id as two plain `NumericDocValues` longs (`chunk_id_hi` /
`chunk_id_lo`) and read it as `new UUID(hi, lo)` — a packed-long access, no dict, no
decompress, no UTF-8 decode, no String, no parse. `page_name` **stays** sorted: it is
genuinely low-cardinality and a shared dictionary is right for it. The `StringField`
term used by `updateDocument`/`deleteDocuments` is untouched.

No migration: the HNSW index is a `ByteBuffersDirectory` rebuilt from
`content_chunk_embeddings` at every boot, so the on-disk encoding question does not
exist. Verified after redeploy — index reloaded 1635 rows, `/api/bundle` returned 12
scored sections.

### The bcrypt burst

The iteration-2 verify cache was working perfectly, and the flat 14% ranking hid
that. Clustering the 141 bcrypt samples **in time** showed three bursts — t=0,
t=+60.5s, t=+122.3s, exactly the 60s TTL — with nothing between them. But each burst
was ~60 simultaneous samples: every request in flight when the entry expired was
independently recomputing the same hash.

**Fix:** `passwordVerifyCache.get(key, loader)` instead of `getIfPresent` + a later
`put`. Caffeine computes the loader under a per-key lock, so the herd shares one
bcrypt. The loader returns `null` on failure, which Caffeine does not store — so a
wrong password is still never cached and still pays full price, and since the key
binds the password hash, each distinct guess is a distinct key. The deterrent is
intact. The method got **shorter**.

After: still 3 bursts at the same TTL boundaries, but ~12 samples each with a uniform
0.28–0.30s span — one bcrypt per expiry, which is exactly the intended cost.

### Results

| | iter 7 | iter 8 | |
|---|---:|---:|---|
| on-CPU samples (total) | 1001 | **781** | **−22%** |
| `topKChunks` | 190 (18.98%) | **68 (8.71%)** | **−64%** |
| `verifySaltedPassword` | 141 (14.09%) | **37 (4.74%)** | **−74%** |
| throughput | 54.67 rps | 54.15 rps | flat |
| p90 / p95 | 24.5 / 44.3 ms | 25.1 / 47.6 ms | flat |
| median | 4.80 ms | 4.24 ms | −12% |
| `http_req_failed` | 2.90% | 2.76% | flat |

Latency and throughput are deliberately flat: at 40 VUs the box is not CPU-saturated,
so removing work shows up as **headroom**, not speed. That headroom is the point — it
is the margin that lets the same instance absorb more traffic, and the reason a cloud
deployment can run on a smaller one.

`read_page` / `read_pages` (item 1) also shipped, as one-line switches to
`getPageWithoutMetadata`. They were 3.8% of the residual `refreshMetadata` work.

### Four pre-existing defects the suite surfaced

None caused by this work; all confirmed at `HEAD` with the changes stashed. Recorded
here because three of them are **date-triggered** and would not have reproduced a
week earlier.

1. **`audit_log` partitions ran out on 2026-09-01.** `V036` pre-created
   `2026-06`..`2026-08` ("current + next two months", written in June). The writer's
   `ensurePartition()` is meant to cover the gap but issues `CREATE TABLE … PARTITION
   OF`, and the documented least-privilege app role has no CREATE on schema public —
   which `JdbcAuditRepositoryTest` exists to pin. **On such a deployment every audit
   write fails with `permission denied for schema public` from 2026-09-01**, and the
   tamper-evident trail silently stops accepting entries. Fixed by `V059`, which
   pre-creates months through 2028-12 in an idempotent `DO` loop. A `DEFAULT`
   partition would also prevent the failure but would then block attaching any future
   monthly partition whose rows had already landed in it — deliberately not used.
2. **`JdbcInsightsStore` date casts were session-timezone dependent.**
   `applied_at::date` on a `TIMESTAMPTZ` resolves in the *session* zone, which pgjdbc
   sets from the JVM default. On any server not running UTC every change date shifts
   by up to a day, mis-aligning the before/after windows `EffectEvaluator` measures
   against. Fixed with `(applied_at AT TIME ZONE 'UTC')::date` at both sites.
3. **Frontmatter `date:` rendered a day early west of Greenwich.** SnakeYAML parses a
   bare `date: 2026-03-20` into a `Date` at UTC midnight; `PageSeoModel` formatted it
   with a default-zone `SimpleDateFormat`, so the page advertised the wrong
   `datePublished` in its JSON-LD and `article:published_time` to search engines. Both
   duplicate helpers now route through one UTC formatter.
4. `KgPolicyCliTest` failed once with a Postgres SSL handshake error and passed 22/22
   in isolation — the known Colima contention flake, not code.

### Remaining, ranked

1. **`FrontmatterParser.parseYaml` at 9.2%** — now the largest application frame.
   SnakeYAML parsing on the page-read path; a parsed-frontmatter cache keyed on body
   hash is the obvious lever, but it needs somewhere to live that is not a new
   `getManager` call site.
2. **`collectLinks` double-parse at 4.6%** — still needs the link-mutator chain
   re-plumbed; unchanged assessment.
3. **`topKChunks` at 8.7%** — now 82% genuine HNSW graph traversal. Further gains
   mean fewer/smaller vectors or a lower `top_k`, both of which trade retrieval
   quality and must go through the nDCG gate. Not a free win any more.
4. **`verifySaltedPassword` at 4.7%** — one bcrypt per 60s TTL per credential. This is
   now the floor, and genuinely an operator dial (`wikantik.auth.password.verifyCache.ttlSeconds`).
