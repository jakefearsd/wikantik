# Wikantik Bootstrapping

Startup and SPI initialization for the Wikantik wiki engine. Manages ordered
engine lifecycle phases, service discovery via `java.util.ServiceLoader`, and
wiring of manager implementations at application startup.
