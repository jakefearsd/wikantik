# Wikantik Cache — Memcached Adapter

Distributed cache adapter backed by Memcached (via XMemcached). Implements
the Wikantik cache abstraction for horizontally-scaled deployments where a
shared distributed cache is preferred over per-node EhCache.
