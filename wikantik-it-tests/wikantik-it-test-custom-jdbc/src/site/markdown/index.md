# Wikantik IT — Custom JDBC Configuration

Integration test WAR for a Wikantik instance configured with JDBC-backed
user management and authentication. Used by the Selenide test suite as the
deployment target for JDBC authentication integration tests.
