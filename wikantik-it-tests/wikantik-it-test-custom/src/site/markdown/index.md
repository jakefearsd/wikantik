# Wikantik IT — Custom Configuration

Integration test WAR for a Wikantik instance configured with custom
properties (filesystem page provider, custom authentication). Used by the
Selenide test suite as the deployment target for custom-configuration
integration tests.
