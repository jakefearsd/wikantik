
# Plain Page

This is a page with no YAML frontmatter at all. It is used by the SemanticWebIT
integration tests to verify that the fallback behaviour works correctly when
frontmatter metadata is absent.
