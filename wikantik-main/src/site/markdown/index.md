# Wikantik Main

Core wiki engine implementation. Contains the `WikiEngine` singleton, all
manager implementations (`PageManager`, `SearchManager`, `RenderingManager`,
etc.), the Markdown rendering pipeline, plugin system, authentication
subsystem, and all business logic for the wiki.
