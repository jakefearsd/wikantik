# Wikantik MCP Server

Model Context Protocol (MCP) server exposing wiki operations as AI-callable
tools. Enables AI agents to read, write, search, and audit wiki content
through a structured JSON-RPC interface backed by the `WikiEngine`.
