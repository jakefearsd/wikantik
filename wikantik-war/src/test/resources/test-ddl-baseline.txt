# Test-DDL single-source baseline (TestSchemaSingleSourceTest).
# See docs/superpowers/plans/2026-08-22-one-way-to-touch-the-database.md
#
# Every test file that still hand-rolls its own CREATE TABLE DDL instead of
# applying the real migrations via com.wikantik.jdbc.testing.PostgresTestDb
# is listed here. Entries only ever come out (as files are migrated onto the
# shared fixture) — never added to paper over a new straggler.
#
# Paths are repo-root-relative, sorted.
# Permanent, justified entry (mirrors the J-1 JDBCPlugin carve-out): the page-authored-SQL
# plugin queries arbitrary user tables, so its CI test creates its own `employees` fixture —
# that is not product schema and has no migration to come from.
wikantik-main/src/test/java/com/wikantik/plugin/JDBCPluginCITest.java
